{
  "dependencies": {
    "@reduxjs/toolkit": "^1.9.1",
    "@tinacms/cli": "^1.0.6",
    "clsx": "^1.2.1",
    "next": "^12.2.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^8.0.3",
    "react-redux": "^8.0.5",
    "react-tinacms-editor": "^0.53.26",
    "sass": "^1.43.4",
    "styled-components": "^5.3.5",
    "tinacms": "^1.1.3"
  },
  "scripts": {
    "dev": "tinacms dev -c \"node server.js\"",
    "build": "tinacms build && next build",
    "export": "tinacms build && next build && next export",
    "start": "tinacms build && next start",
    "lint": "next lint"
  },
  "devDependencies": {
    "eslint": "8.26.0",
    "eslint-config-next": "12.3.1"
  }
}
